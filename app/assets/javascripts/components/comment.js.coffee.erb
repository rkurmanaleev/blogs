@Comment = React.createClass
  getInitialState: ->
    comment: @props.data
  getDefaultProps: ->
    comment: []
  render: ->
    console.log(@props.data)
    React.DOM.div
      className: "list-comments"
      React.DOM.div
        className: "row"
        React.DOM.div
          className: "col-sx-2 col-sm-2 col-md-2 col-lg-2"
          React.DOM.img
            src: "<%= asset_url('no-avatar.png') %>"
            width: "50x50"
            alt: "User's avatar"
          React.DOM.div
            className: "text-left"
            "Blabla"
        React.DOM.div
          className: "col-sx-7 col-sm-7 col-md-7 col-lg-7"
          "#{@props.data.content}"
            React.DOM.div
              className: "comment-info"
              React.DOM.div
                className: "comment-message"
                "Created by #{@props.data.user_id} on #{@props.data.created_at}"
